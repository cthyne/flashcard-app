import React from "react"

// return "not found" message if no route is found for user
function NotFound() {
  return (
    <div className="NotFound">
      <h1>Not Found</h1>
    </div>
  )
}

export default NotFound
